//import { createSelector } from '@reduxjs/toolkit';

// export const getStatusDialog = (state) => state.dialog.isOpen;
// export const getContentDialog = (state) => state.dialog.content;
// export const getOptionsDialog = (state) => state.dialog.options;
export const getDialogItems = (state) => state.dialog.dialogItems;
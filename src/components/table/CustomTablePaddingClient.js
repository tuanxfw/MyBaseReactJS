import React from "react";

const CustomTablePaddingClient = (props) => {
    return (
        <div className="tooltip-validate">
            
        </div>

    );
}

export default CustomTablePaddingClient;

import React from "react";
import BootstrapTable from "react-bootstrap-table-next";

const CustomTablePagingApi = (props) => {
    return (
        <div className="tooltip-validate">
            
        </div>

    );
}

export default CustomTablePagingApi;
